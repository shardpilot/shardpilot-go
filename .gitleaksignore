# Six generic-api-key hits on 3bf08ff8f6, contained in refs/pull/39/head and no
# branch or tag. Classified 2026-08-24 as NOT credentials: each value is an
# ordered counting run or a descriptive test token — a property of the value,
# not a reading of its purpose. Recorded in the qa publication gate's
# SECRET_SCAN_2026_08_24 cohort as findings: 6 with that remainder.
# Pinned per finding, not allowlisted by shape: these entries die when the
# commit stops being advertised, and forgive nothing anywhere else.
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiment_events_test.go:generic-api-key:67
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiment_events_test.go:generic-api-key:337
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiments_test.go:generic-api-key:17
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiments_test.go:generic-api-key:24
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiments_test.go:generic-api-key:187
3bf08ff8f6f46bca38f7f42513e9bd2b086e5402:experiments_test.go:generic-api-key:611

# One generic-api-key hit on 626d0f95, in refs/pull/73/head and no branch or tag.
# Classified 2026-08-29 as NOT a credential: a made-up subject identifier in a
# test fixture — `spcid_` followed by sequential hex digits — asserting that a
# redirect `Location` echoing the request URL gets scrubbed. `spcid_` values are
# SDK-minted subject ids, never access credentials; the rule fired on the entropy
# of the literal, not on its meaning. shardpilot-go#73 replaced the fixture with
# a low-entropy value of the same wire shape, so no tree contains it.
#
# ⚠ IT IS PINNED ON `main` BECAUSE THE SCAN IS REPOSITORY-WIDE, NOT BRANCH-WIDE.
# ci.yml fetches `+refs/pull/*/head:refs/remotes/origin/pr/*` before scanning, so
# EVERY pull request's run sees every other pull request's commits. A pin that
# lives only on the branch that introduced the finding silences it there and
# nowhere else — shardpilot-go#73 was green while #77 was red for a commit #77
# does not contain. An ignore entry has to sit where every run reads it.
#
# Pinned per finding, not allowlisted by shape, per this file's rule: this entry
# dies when the commit stops being advertised and forgives nothing anywhere else.
626d0f95da37c5b853cb9adf02772ffaf828741c:examples/experiments/main_test.go:generic-api-key:199

# One generic-api-key hit on fa107e25, in refs/pull/73/head and no branch or tag.
# Classified 2026-08-29 as NOT a credential: a 38-character hex literal in a test
# fixture, standing in for an SDK-minted `spcid_` subject id so the fixture can
# check that a GENERATED placeholder is not read as a leak. The rule fired on the
# entropy of the literal, not on its meaning.
#
# ⚠ SECOND ENTRY OF THIS EXACT SHAPE TODAY, and by the same author. The first
# (626d0f95, above) taught the cure -- pin by finding, never by shape -- and not
# the prevention, so a high-entropy literal went into another fixture four hours
# later. shardpilot-go#73 has replaced it with a repeating low-entropy value of
# the same length, so no tree contains it; only the pull ref does, and only until
# that ref stops being advertised.
#
# The prevention, written here because this file is where the recurrence is
# visible: a fixture needs a value's SHAPE, never its randomness. Repeat a short
# pattern to the required length.
fa107e25a30f2e0345fecacbc600ba7735b66ed2:examples/experiments/main_test.go:generic-api-key:398

#
# ⚠ THIRD ENTRY, SAME FILE, SAME RULE, SAME AUTHOR, SAME DAY. The prevention two
# entries up is correct -- a repeating low-entropy value of the same length
# measures clean on gitleaks 8.30.1, checked before this line was written -- and
# it did not fire, because it lives HERE and this file is only ever read AFTER a
# failure. The reminder now also sits at the fixture, which is where the author
# of the next one is looking. Pinned by finding, as always: this dies with the
# commit rather than forgiving the shape anywhere.
#
# ⚠ NOT THIS BRANCH'S FINDING, AND THAT IS THE POINT. `ci.yml` fetches
# `+refs/pull/*/head` before scanning history, so a fixture on ANY open pull
# request reddens EVERY open pull request. This pin is for a commit on
# shardpilot-go#73, carried here because the gate this branch must pass reads
# that commit too. It dies when that ref stops being advertised, exactly like
# every other entry -- pinned by finding, never by shape or path.
4a63dd612905af4d4702d70e39d22b93806ed7ab:examples/experiments/main_test.go:generic-api-key:697
