# Allocation baseline — Go SDK, 2026-08-28

- Date: **2026-08-28**
- Commit measured: `9db83d94336d384434d2fa47cddc65af37d07cd1` (`origin/main`)
- Toolchain: `go1.27.0 darwin/arm64`
- Machine: Apple M1 Max, 10 CPUs, macOS 26.5.2 — **a laptop, not the CI runner**
- Produced by: `SHARDPILOT_BENCHTIME=3s scripts/run_benchmarks.sh`

## Why this file exists at all

Automatic session-lifecycle events are planned for this SDK. They change
allocation, batching cadence and event volume, so **once they land, "what did
this cost before" is unrecoverable.** The measurement had to be taken now or
not at all.

There was no series to append to. `/bench/` is gitignored — anchored,
deliberately, as CI working output — `git ls-files bench/` returns 0, and the CI
artifact carries `retention-days: 30`. Every number this repository has ever
produced has expired. `scripts/run_benchmarks.sh:18-21` already says what is
missing:

> "a threshold needs a baseline measured on hardware comparable to the runner …
> a regression gate is a separate decision"

This is the first dated entry of that series. It is deliberately **not** in
`/bench/`, which would ignore it.

## The numbers, two independent runs

| Benchmark | ns/op r1 | ns/op r2 | spread | B/op r1 | B/op r2 | allocs/op |
|---|---:|---:|---:|---:|---:|---:|
| `EnqueueMapPayload` | 3 114 | 1 111 | **64.3 %** | 672 | 672 | **4 / 4** |
| `BuildBatchMapPayloads` | 619 115 | 576 459 | 6.9 % | 162 750 | 162 756 | **2306 / 2306** |
| `StatsCollectorAggregation` | 22.97 | 19.43 | 15.4 % | 0 | 0 | **0 / 0** |
| `CompressBatchBody` | 29 302 | 43 503 | 32.6 % | 6 848 | 6 854 | **2 / 2** |
| `ClientEmit` | 99 489 | 59 002 | 40.7 % | 7 653 | 7 663 | **72 / 72** |

`CompressBatchBody` also reports throughput: 461.34 MB/s and 310.74 MB/s.

## The load-bearing result is not any single number

**`ns/op` on this machine is not a usable baseline axis.** Run-to-run spread
between two consecutive runs of unchanged code, same binary, same benchtime, is
**6.9 % to 64.3 %**. A later comparison against a changed SDK could show a 40 %
regression that is entirely noise, and — worse — could hide a real 30 % one.

**`allocs/op` is stable, and "stable" is not "exact".** Go computes both
`allocs/op` and `B/op` by integer-dividing the totals by `b.N`, so what is
reported is a **truncated per-operation average**, not a count. Two runs showing
`4 / 4` are consistent with an allocation that happens once per client lifetime,
or intermittently on one iteration in a thousand — the division hides it, and
this file does not retain the raw totals from which it could be recovered.

So the axis is the *best available* one here, not a certain one. A future
comparison that needs to detect a once-per-lifecycle allocation — exactly the
shape automatic lifecycle events would introduce — must keep `b.N` and the raw
totals, not the formatted averages.

So the axis a future comparison must use is **allocations and bytes**, and any
wall-time claim needs either the CI runner or a many-run distribution, neither of
which this file provides.

## A second instrument note

The runner pins `-benchtime=100x` by default (`run_benchmarks.sh:416-420`), for
CI determinism. That pin makes it useless as a baseline. Measured at `100x`
against the same commit:

| Benchmark | ns/op at 100x | ns/op at 3s (r1) | divergence |
|---|---:|---:|---:|
| `EnqueueMapPayload` | 1 257 | 3 114 | 2.5× |
| `BuildBatchMapPayloads` | 327 905 | 619 115 | 1.9× |
| `StatsCollectorAggregation` | 98.33 | 22.97 | 4.3× |
| `CompressBatchBody` | 22 046 | 29 302 | 1.3× |
| `ClientEmit` | 57 362 | 99 489 | 1.7× |

Allocation counts were identical at both settings.

**And the divergence has a mechanism, which is better than the "inconsistent
bias" this file first claimed.** The two settings do not run the same amortized
workload:

- `BenchmarkStatsCollectorAggregation` snapshots at `i % 256 == 0`. At `N = 100`
  that is one snapshot in a hundred operations; at a settled `N` it is one in
  256. The per-operation cost is structurally different.
- `BenchmarkEnqueueMapPayload` drains only when its 1024-deep queue fills. At
  `N = 100` it **never drains**; at a settled `N` it drains repeatedly.

So the 4.3× is not the instrument wobbling — it is two different workloads
wearing one name. The practical conclusion survives and its reason improves: a
`100x` run is not a smaller version of a settled run, so it cannot be a baseline
for one.

Uniform `N = 100` across all five benchmarks is the signature of a broken
instrument, and was checked as one before being trusted: it is the deliberate pin
at line 420, not a failure.

## Axes, including the ones not varied

**These are not the SDK's default configuration, and the header should not be
read as saying so.** `BenchmarkEnqueueMapPayload` builds a partial `Client`
literal with only a queue — bypassing the constructor and every config default.
`BenchmarkBuildBatchMapPayloads` supplies an explicit `Config` pinning
`Source: SourceBackend`. The crash benchmark supplies its own options, transport
and sampler. Where later behaviour is governed by constructor defaults or by
`source`, these numbers describe a different configuration than a real client's.

Varied: benchtime (`100x`, `3s`), and repetition (two runs at `3s`).

**Not varied, and each is a real limit on what this file can be compared to:**

- **One machine.** Apple M1 Max laptop. Not the CI runner, so these numbers
  cannot be compared to anything CI produces, in either direction.
- **One OS and architecture.** `darwin/arm64` only. No Linux, no amd64.
- **One toolchain.** `go1.27.0`.
- **Thermal state uncontrolled.** Two runs, back to back, on a machine doing
  other work — which is the most likely source of the `ns/op` spread above.
- **One SDK.** Go only. The four engine SDKs answer device-shaped questions
  (frame cost, battery) that this harness cannot ask, and their baseline is not
  in this file.
- **Config not varied.** Whatever `origin/main` defaults to at the commit above.

## What this does and does not establish

Establishes: the allocation and byte cost of the Go SDK's five tracked
benchmarks, on this machine, at this commit, before automatic collection.

Does **not** establish: any wall-time figure durable enough to compare against;
anything about the four engine SDKs; anything about behaviour on the CI runner;
anything about battery or frame cost, which is the question studios actually ask
and which a later device-side measurement owes.

**And it does not exercise the lifecycle path at all — the largest gap in this
file.** Automatic session events act at client construction and shutdown, and
they raise the number and cadence of batches. None of the five benchmarks
observes that: two construct clients directly and measure isolated primitives,
two run on fixed inputs, and the crash benchmark belongs to another package.
A per-operation figure cannot see a cost paid once per client, and a fixed-input
benchmark cannot see a change in how often batches are built.

That is a real limit on this file's stated purpose. It prices the operations
automatic collection will make **more numerous**; it does not price the
operations automatic collection will **add**. The final measurement owes a
benchmark that drives a client through construction, activity and shutdown —
which cannot be written before that lifecycle exists, and is named here so it is
not mistaken for covered.
